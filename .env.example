# .env.example

# Backend (FastAPI) secrets
JWT_SECRET_KEY=your-strong-random-hex-or-urlsafe-key
SENTRY_DSN=your-backend-sentry-dsn

# Frontend (Vite) settings
# During local Docker dev, point at the backend on localhost:8000
VITE_API_BASE_URL=http://localhost:8000
VITE_SENTRY_DSN=your-frontend-sentry-dsn
