# This is an example environment file for local development using Docker Compose.
# Copy this file to .env and fill in your actual values.
# Do NOT commit your .env file to version control.

# --- Backend Configuration ---
# Used by packages/backend/main.py for JWT signing and verification.
# Generate a strong secret key, e.g., using: openssl rand -hex 32
JWT_SECRET_KEY="your_super_secret_random_32_byte_hex_key_here_for_jwt"

# Used by packages/backend/main.py if Sentry is initialized.
# Get this DSN from your Sentry project settings for the backend.
SENTRY_DSN="your_backend_sentry_dsn_here_if_using_sentry"


# --- Frontend Build-Time Configuration ---
# These variables are used by the frontend build process (e.g., Vite)
# and can be passed as build arguments in docker-compose.yml or Dockerfile.

# The base URL for the backend API that the frontend will call.
# For local development with docker-compose where backend port 8000 is mapped to host:
VITE_API_BASE_URL="http://localhost:8000"

# Used by packages/frontend/src/main.jsx if Sentry is initialized.
# Get this DSN from your Sentry project settings for the frontend.
VITE_SENTRY_DSN="your_frontend_sentry_dsn_here_if_using_sentry"
