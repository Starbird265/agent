# This is an example environment file.
# Copy this file to .env and fill in your actual values.
# Do NOT commit your .env file to version control.

# For JWT Authentication (packages/backend/main.py)
# Generate a strong secret key, e.g., using: openssl rand -hex 32
JWT_SECRET_KEY="your_super_secret_random_32_byte_hex_key_here"

# Optional: If you still have parts of the code that might use the old STATIC_API_KEY
# via os.getenv("API_KEY", "dev_secret_key") and you want to override the default:
# API_KEY="your_optional_static_api_key_override"
